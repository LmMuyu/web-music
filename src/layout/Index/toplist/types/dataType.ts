export interface ChildrenDataItme {}

export interface ListTitle {
  cloud: {
    index?: number;
    title: string;
    childrenData: any[];
  };
  global: {
    index?: number;
    title: string;
    childrenData: any[];
  };
}
